{% extends 'blog/layout-blog.html.twig' %}

{% block body %}
    <div class="login-container">
        <h2 class="title is-3">Connexion</h2>
        <div class="border-form">
            {% include('blog/security/_login_form.html.twig') %}

            <p class="login-subtitle">Pas encore inscrit ? <a href="{{ path('registration') }}">Créer un compte</a></p>
        </div>
    </div>
{% endblock %}
