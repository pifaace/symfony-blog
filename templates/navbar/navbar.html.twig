<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="container">

        <div class="navbar-brand">
            <a href="{{ path('homepage') }}" class="navbar-item">Training Symfony-blog</a>

            <div class="button navbar-burger burger">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>

        <div class="navbar-menu">
            {% if is_granted('ROLE_ADMIN') %}
                <div class="navbar-start">
                    <a href="{{ path('admin-dashboard') }}" class="navbar-item">
                        Dashboard
                    </a>
                </div>
            {% endif %}
            <div class="navbar-end">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <div class="navbar-item has-dropdown is-hoverable">
                        <a class="navbar-link">
                            {{ app.user.username }}
                        </a>
                        <div class="navbar-dropdown is-right">
                            <a class="navbar-item" href="#">
                                Profil
                            </a>
                            <a class="navbar-item" href="{{ path('logout') }}">
                                Déconnexion
                            </a>
                        </div>
                    </div>
                {% else %}
                    <a href="{{ path('login') }}" class="navbar-item navbar-action bold">Connexion</a>
                    <p class="navbar-item navbar-action">ou</p>
                    <a href="{{ path('registration') }}" class="navbar-item navbar-action bold">Inscription</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
